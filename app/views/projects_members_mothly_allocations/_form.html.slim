= form_for @projects_members_mothly_allocation do |f|
  - if @projects_members_mothly_allocation.errors.any?
    #error_explanation
      h2 = "#{pluralize(@projects_members_mothly_allocation.errors.count, "error")} prohibited this projects_members_mothly_allocation from being saved:"
      ul
        - @projects_members_mothly_allocation.errors.full_messages.each do |message|
          li = message

  .field
    = f.label :project
    = f.text_field :project
  .field
    = f.label :member
    = f.text_field :member
  .field
    = f.label :month
    = f.text_field :month
  .field
    = f.label :cost
    = f.text_field :cost
  .actions = f.submit
