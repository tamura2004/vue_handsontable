htbl ctl="projects" :rows=@project[:records].to_json :opts=@project[:options].to_json
hr

htbl ctl="members_allocations" :rows=@members_allocations[:records].to_json :opts=@members_allocations[:options].to_json
hr

= link_to "要員アサイン", project_members_path(@project), class: "btn btn-primary"
