.page-header
  h3
    = @job_title.name
    small = @group.name

h5 要員
htbl ctl="works" :rows=@data[:works][:records].to_json :opts=@data[:works][:options].to_json
hr

h5 アサイン
htbl ctl="members_projects_months" :rows=@data[:members_projects_months][:records].to_json :opts=@data[:members_projects_months][:options].to_json
hr

h5 案件
htbl ctl="projects_members_months" :rows=@data[:projects_members_months][:records].to_json :opts=@data[:projects_members_months][:options].to_json
